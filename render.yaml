services:
  - type: worker
    name: davex-ultra
    runtime: docker
    repo: https://github.com/Davex-254/DAVE-X-ULTRA
    branch: main
    dockerfilePath: ./Dockerfile
    dockerContext: .
    envVars:
      - key: SESSION_ID
        sync: false
